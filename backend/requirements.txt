fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlmodel==0.0.22
pydantic-settings==2.7.1
httpx==0.28.1
selectolax==0.3.27
apscheduler==3.11.0
python-dotenv==1.0.1

# Gmail integration
google-api-python-client==2.156.0
google-auth==2.37.0
google-auth-oauthlib==1.2.1

# Postgres driver — only needed if you set DATABASE_URL to a Postgres URL. The default
# deploy uses SQLite (stdlib), so this stays commented to keep the install lean on a
# 1 GB VM. Uncomment if you switch to Postgres.
# psycopg[binary]==3.2.3

# Tests
pytest==8.3.4
pytest-asyncio==0.25.0
